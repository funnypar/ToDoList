import styles from "./Main.module.css";
import Input from "../Input/Input";

const Main = () => {
    return (
        <div className={styles.container}>
            <Input />
        </div>
    );
};

export default Main;
